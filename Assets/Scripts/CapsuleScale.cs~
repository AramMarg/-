using UnityEngine;

public class CapsuleScale : MonoBehaviour
{
    [SerializeField] private float _scaleSpeed;

    private Vector3 _scaleIncrease = new(1,1,1);

    private void Update()
    {
        transform.lossyScale.Scale(_scaleIncrease * _scaleSpeed * Time.deltaTime);
    }
}
